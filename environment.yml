# Anaconda / Miniconda (macOS Apple Silicon, Linux, Windows):
#   conda env create -f environment.yml
#   conda activate soccercal
name: soccercal
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - torchvision
      - -e .[notebooks,dev]
